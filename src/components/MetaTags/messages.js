/*
 * MetaTags Messages
 *
 * This contains all the text for the MetaTags component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MetaTags';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MetaTags component!',
  },
});
