/*
 * Merits Messages
 *
 * This contains all the text for the Merits container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Merits';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Merits container!',
  },
});
