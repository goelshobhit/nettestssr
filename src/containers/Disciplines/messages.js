/*
 * Disciplines Messages
 *
 * This contains all the text for the Disciplines container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Disciplines';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Disciplines container!',
  },
});
