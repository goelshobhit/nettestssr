/*
 * Flaw Messages
 *
 * This contains all the text for the Flaw container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Flaw';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Flaw container!',
  },
});
