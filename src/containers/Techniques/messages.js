/*
 * ClanPage Messages
 *
 * This contains all the text for the ClanPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClanPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClanPage container!',
  },
});
